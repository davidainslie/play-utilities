package controllers

class MappingsSpec {
  /*
  object MyController extends Controller with Mappings {
    val form = Form(mapping(
      "done" -> requiredBoolean
  )(SomeDomainObject.apply)(SomeDomainObject.unapply))
   */
}